# Relative: the Vite dev server proxies /api to http://localhost:5000
# (see vite.config.js), and in production IIS proxies /api to Node.
# Same-origin everywhere = no CORS.
VITE_API_URL=/api
VITE_RAZORPAY_KEY_ID=your_razorpay_key_id
