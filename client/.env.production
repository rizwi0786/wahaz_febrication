# Relative on purpose: API calls stay same-origin no matter which host serves
# the page (apex or www), so they can never be CORS-blocked. IIS proxies /api
# to the Node server.
VITE_API_URL=/api
VITE_RAZORPAY_KEY_ID=rzp_live_T4Audapt2ostSE
